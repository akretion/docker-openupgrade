networks:
  traefik:
    external: true
  local:
services:
  db:
    image: postgres:13
    user: "${UID}:${UID}"
    environment:
      - POSTGRES_USER=odoo
      - POSTGRES_DB=openupgrade16
      - POSTGRES_PASSWORD=odoo
    volumes:
      - .db/:/var/lib/postgresql/data
    networks:
      - local
  odoo:
    build: odoo
    environment:
      PGDATABASE: openupgrade16
      PGHOST: db
      PGPASSWORD: odoo
      PGUSER: odoo
      PYTHONDONTWRITEBYTECODE: "True"
      LOCAL_USER_ID: $UID
      LIMIT_TIME_CPU: 6000000
      LIMIT_TIME_REAL: 12000000
      LIMIT_MEMORY_SOFT: 5147483648
      LIMIT_MEMORY_HARD: 5684354560
      ADDONS_PATH: /odoo/links,/odoo/local-src,/odoo/src/odoo/addons,/odoo/src/addons,/odoo/external-src/openupgrade
    links:
      - db
    networks:
      - local
    volumes:
      - ./odoo:/odoo
      - ./data/addons:/data/odoo/addons
      - ./data/filestore:/data/odoo/filestore
      - ./data/sessions:/data/odoo/sessions
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik"
      - "traefik.http.routers.openupgrade16.rule=Host(`openupgrade16.localhost`)"
      - "traefik.http.routers.openupgrade16-long.rule=Host(`openupgrade16.localhost`)
        && PathPrefix(`/longpolling/`)"
      - "traefik.http.routers.openupgrade16.service=openupgrade16odoo"
      - "traefik.http.routers.openupgrade16-long.service=openupgrade16odoo_long"
      - "traefik.http.services.openupgrade16odoo.loadbalancer.server.port=8069"
      - "traefik.http.services.openupgrade16odoo_long.loadbalancer.server.port=8072"
      - "traefik.http.middlewares.openupgrade16corsHeader.headers.accessControlAllowMethods=
        GET,POST,OPTIONS,PUT"
      - "traefik.http.middlewares.openupgrade16corsHeader.headers.accessControlAllowOriginList=*"
      - "traefik.http.middlewares.openupgrade16corsHeader.headers.accessControlAllowHeaders=DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,api-key,partner-email,authorization,sess-cart-id,website-unique-key"
      - "traefik.http.middlewares.openupgrade16corsHeader.headers.accessControlExposeHeaders=Content-Length,Content-Range"
      - "traefik.http.middlewares.openupgrade16corsHeader.headers.addVaryHeader=true"
      - "traefik.http.routers.openupgrade16.middlewares=openupgrade16corsHeader"
      - "docky.help=http://openupgrade16.localhost"

version: '3'
